apiVersion: apps/v1
kind: Deployment
metadata:
  name: summarization-service
  labels:
    app: summarization-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: summarization-service
  template:
    metadata:
      labels:
        app: summarization-service
    spec:
      containers:
      - name: summarization-service
        image: docker.io/library/summarization-service:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8001
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 60
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: summarization-service
spec:
  selector:
    app: summarization-service
  ports:
  - port: 8001
    targetPort: 8001
  type: ClusterIP
